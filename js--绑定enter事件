
//绑定登录点击事件，submit
$(document).on("click", "#J_sbmbtn", function() {
    self.loginFun.call($(this));
 });


  //绑定enter事件，触发登录的点击事件，等同提交
  $(document).keydown(function(event) {

    if (event.keyCode == 13) {

      $("#J_sbmbtn").trigger('click');
    }
  });
